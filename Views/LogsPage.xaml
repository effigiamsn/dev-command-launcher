<Page
    x:Class="DevCommandLauncherApp.Views.LogsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Padding="28,10,28,24" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" ColumnSpacing="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="320" />
                <ColumnDefinition Width="260" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <ComboBox
                Grid.Column="0"
                ItemsSource="{Binding LogCommandOptions}"
                DisplayMemberPath="DisplayName"
                SelectedItem="{Binding SelectedLogCommand, Mode=TwoWay}" />
            <TextBox
                Grid.Column="1"
                PlaceholderText="Search logs"
                Text="{Binding LogFilter, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <Button Grid.Column="2" Width="40" Height="36" Click="OnClearLogsClick">
                <ToolTipService.ToolTip>Clear Logs</ToolTipService.ToolTip>
                <SymbolIcon Symbol="Delete" />
            </Button>
        </Grid>

        <Border
            Grid.Row="1"
            Padding="14"
            CornerRadius="8"
            Background="#111827">
            <TextBox
                Text="{Binding LogText, Mode=OneWay}"
                AcceptsReturn="True"
                TextWrapping="NoWrap"
                IsReadOnly="True"
                FontFamily="Cascadia Mono, Consolas, Malgun Gothic"
                FontSize="13"
                Foreground="#E5E7EB"
                Background="Transparent"
                BorderThickness="0"
                ScrollViewer.VerticalScrollBarVisibility="Auto"
                ScrollViewer.HorizontalScrollBarVisibility="Auto" />
        </Border>
    </Grid>
</Page>
